{% extends "index" %}

{% set user = craft.users.username(slug).group('participants').one() %}

{% block content %}

	{% if user %}
		<pre>{{ dump(user) }}</pre>
	{% else %}
		{% redirect "/" %}
	{% endif %}

{% endblock %}
